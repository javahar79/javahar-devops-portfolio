#!/usr/bin/env bash
# last_verified: 2026-08-14 · docker n/a

# My first Docker CLI commands: pull, run, list, and inspect a container.

docker pull hello-world
docker run hello-world
docker ps -a
docker inspect hello-world

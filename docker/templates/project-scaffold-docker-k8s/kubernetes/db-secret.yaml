# last_verified: 2026-08-14 · kubernetes n/a
# Placeholder secret consumed by the db StatefulSet. For local/dev clusters
# only — replace the value with a real secret manager in a shared environment.
# The password is base64-encoded (echo -n "change-me" | base64).
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  labels:
    app: multi-service
type: Opaque
data:
  password: Y2hhbmdlLW1l
